# RAG-Time API — dépendances minimales
# Installer : pip install -r requirements_api.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# LLM (client OpenRouter)
openai>=1.0.0

# Vectorstore
faiss-cpu>=1.8.0

# Embeddings — modèle local léger (~118 MB, 384 dims, multilingue)
sentence-transformers>=3.0.0

# Core scientific
numpy>=1.26.0
