# RAG-Time — Dépendances
# Installation : pip install -r requirements.txt

# LLM via OpenRouter (API compatible OpenAI)
openai==1.82.0

# Vector store (recherche vectorielle exacte, pre-built wheels)
faiss-cpu==1.13.2

# Embeddings via HuggingFace Inference API (serverless, zero RAM local)
# appels HTTP avec HF_API_KEY -- aucun modele charge localement
huggingface-hub>=0.23.0

# Progress bars (ingestion + évaluation)
tqdm

# Frontend
streamlit==1.41.1

# Data
pandas==2.2.3

# Lecture PDF
pypdf==5.1.0

# Variables d'environnement
python-dotenv==1.0.1
